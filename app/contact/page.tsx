import type {Metadata} from "next";
import {ContactSection} from "@/components/sections";
export const metadata:Metadata={title:"Contact",description:"Send a product, sourcing, packaging, distribution, or partnership brief to LIXIANG Trade."};
export default function ContactPage(){return <ContactSection/>}
